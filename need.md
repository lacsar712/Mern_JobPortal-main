3.6 交互约束与轮次限制（本期有改动）
交互轮次至少 3 轮，最多 6 轮 （$$3 \leq 轮数 \leq 6$$）。原因是：
- 开Max后是1M窗口，需要多轮才能达到长上下文窗口：单轮任务很难把上下文堆进长上下文区间，上期我们开了Max的题里很多都只跑了1轮，导致长上下文样本不足。因此本期要求至少跑 3 轮：请在出题时就把题目设计成需要3轮以上才能完成（第 2、3 轮刻意加入跨轮次依赖，手法可以参考下方3.7的“跨轮长上下文出题示例”），提前设计的目的是保证四个模型接到的是一样的任务。
- 控制评测成本（AHT）：不设上限会让评测工时不可控，也违背快速打分的初衷，因此我们仍保留上限
- 防止 PE 模糊模型真实能力：避免专家的提示词水平掩盖模型的真实纠错能力。
例外情况：
- 对于显然的工程故障（网络波动导致请求失败），可以不计入轮次。
- 但是，对于轮次超限需要人为“继续”的情况，应计入轮次。
3.7 上下文压力出题建议（本期新增记录要求）
本期我们为了考察模型的长上下文多轮能力，希望大家可以在出题以及多轮交互过程中，通过难题或对query的设计尽可能地让任务接近 context window上限，我们在本节提供了 上下文档位分布的建议以及一些题目设计技巧 可供大家参考。
上下文档位-出题分布建议（分布不做强制要求）
上下文档位
占用目标（按 1M 窗口）
占比建议
（不做强制要求）
定位
日常短窗口
<200K（<20% of 1M）
30%
日常高频短任务，此档不打长上下文分
长上下文
200K–600K（20%–60% of 1M）
30%
长上下文区，可观察退化拐点
超长上下文（1M）
≥600K（≥60% of 1M）
40%
近1M的压力考察（本期重点）
打分阈值：只有context≥200K 的样本，才需要打“长上下文保持能力”维度的分数（详见第四部分的打分方法）
上期我们发现的问题：开了Max的题平均只跑了1.9 轮、49%是单轮，最终只有31%的样本进入长上下文区间，所以本期要求至少跑 3 轮（详见 3.6），并在第 2、3 轮设计跨轮次需求
如何达到长上下文目标区间（仅供参考）
- 导入大型真实 repo（数十万行、多包monorepo），要求模型在完整仓库上下文下作业
- 要求模型通读/跨越多个大文件，或在Prompt中贴入超长规格文档（PRD/API doc/长日志）
- 通过长程多轮任务累计上下文（本期鼓励大家用多轮对话观察模型在长上下文下的能力）
跨轮长上下文出题示例
我们建议专家在本期众测中，应优先设计 需要依赖早期轮次信息 的多轮题（开Max时 3–6 轮，未开Max时 ≤3 轮），让模型的记忆与检索能力在长上下文下能够被真实考察。同时在设计这类题目时，尽量让早期轮次信息在后面轮次不可省略，并同步自己记录一下这个关键依赖信息，便于评分时判断模型是否真的记住了早期上下文而非重新读取，这里提供几个示例：

出题手法
具体示例
考察点
1
跨轮依赖
第1轮约定数据 schema/命名规范/接口签名，第4+轮新增功能时必须复用第1轮的约定
早期约束是否被长期记住
2
渐进式大系统构建
逐轮增量搭建一个大型系统，每一轮新增模块都必须与前几轮产出的接口、数据结构、命名等保持一致
上下文累积下的一致性
3
长文档needle分布
第1轮贴入超长PRD/API文档，后续各轮分别引用文档不同位置（开/中段/结尾）的信息
Lost-in-the-middle, 中段信息检索
4
埋点约束回溯
前面几轮埋入一条全局约束（比如“所有金额一律以分为单位”或者“禁止用any类型”），多轮后再出新功能，看模型是否还遵守这个约束
长距离约束保持
5
长链路多轮调试
多轮排查横跨前后端/多服务的bug，每轮贴入新的报错日志/堆栈，历史日志需保留参考才能定位根因
长历史下的推理与检索
6
回溯校验
在较后轮次要求模型 回顾并总结此前所有轮次的改动，做一致性检查并列出冲突点等
主动调用全量早期上下文
新增context占用记录要求
- 每题每个模型都需要记录context占用，读取 Trae SOLO Agent 对话底部的 上下文使用率 X% of YK。
- 本期增加了是否触发自动压缩的字段，并统一“context占用”这个字段的记录格式（分为触发压缩/未触发压缩两种）
- 记录规则 & Trae SOLO Agent 端的 context 位置如下所示：
📝 记录规则 📝
场景
Context占用 记录说明
通用规则
记录最后一轮的读数，但请过程中时刻注意是否发生了压缩，每轮结束时可以都看一眼底部的X% of YK
未触发自动压缩
格式：X% of YK
例：28% of 1000K）
触发了自动压缩
- 由于压缩会把上下文从高位压回低位，最后一轮读数不是真实峰值，所以我们需要把压缩前的读数一并记下来
- 格式：X% of YK，触发自动压缩时 Z% of YK
- 例：12% of 1000K，触发自动压缩时61% of 1000K
⚠️ Max自检
开启Max后窗口应显示 X% of 1000K。如果看到的是 of 186K / of 224K 这类小窗口，说明 Max 没有生效，请检查配置后重测这一题
📍 显示位置 📍
[图片]

3.8 Max模式 (支持1M上下文) 分布要求
本期要求有所改动：所有任务都要开启Max（启用1M上下文窗口）
怎么确认 Max 真的生效了：开启后对话底部的上下文使用率会显示为 X% of 1000K。如看到窗口不是 1000K，请立即检查配置并重测